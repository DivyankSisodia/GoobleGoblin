name: gooble_goblin
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  fl_chart: ^1.1.1
  flutter:
    sdk: flutter
  flutter_3d_controller: ^2.3.0
  google_fonts: ^6.3.3
  local_auth: ^3.0.0
  logger: ^2.6.2
  sqflite: ^2.4.2
  syncfusion_flutter_chat: ^32.1.21

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
